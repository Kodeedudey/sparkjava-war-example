version: 0.2

phases:
  install:
    commands:
      runtime-versions:
        java: openjdk8
        install maven
  build:
    commands:
      - echo Build started on `date`
      - mvn test
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
